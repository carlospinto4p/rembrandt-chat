---
description: Audit user-facing strings in i18n.py for grammar, punctuation, and natural phrasing
---

<!-- opencode-side shim of the Claude Code skill
     .claude/skills/i18n-review/SKILL.md — skills are loaded by
     opencode but are NOT `/` slash commands in its TUI, so this shim
     provides the `/i18n-review` surface. Keep in sync with that
     file (see the global claude-opencode-sync rule). -->

Invoke the `i18n-review` skill via the skill tool and follow it
exactly. Arguments: $ARGUMENTS
