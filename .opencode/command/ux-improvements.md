---
description: Audit every user-facing interaction in the Telegram bot and suggest UX improvements
---

<!-- opencode-side shim of the Claude Code skill
     .claude/skills/ux-improvements/SKILL.md — skills are loaded by
     opencode but are NOT `/` slash commands in its TUI, so this shim
     provides the `/ux-improvements` surface. Keep in sync with that
     file (see the global claude-opencode-sync rule). -->

Invoke the `ux-improvements` skill via the skill tool and follow it
exactly. Arguments: $ARGUMENTS
